CityPulse frontend (minimal Vite + React app)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173 and ensure the backend is running at http://127.0.0.1:8000.
